// Array Methods : push,pop,shift,unshift,splice,slice,reverse,sort,forEach,map,filter,reduce,find,some,every

let arr = [1, 2, 3, 4, 5];

// arr.push(6)
// arr.pop()
// arr.shift()
// arr.unshift()
// arr.splice(2,1)
// let newArr = arr.slice(2,1)
// arr.reverse()

// let newArr = arr.sort(function (a, b) {
//   return a - b;
// });

// arr.forEach(function(val){
//     console.log(val + 1);
// })

// let newArr = arr.map(function (val) {
//   return 5
// });

// let newArr = arr.filter(function (val) {
//   if (val > 2) return true;
// });

// let newArr = arr.reduce(function(accumulator, val){
//     return accumulator + val
// }, 0) ---- here accumulator val is set to 0, accumulator holds its value

// let newArr = arr.find(function (val) {
//   return val === 1;
// });

// let newArr = arr.some(function (val) {
//   return val > 4;
// }); --- returns true/false

// let newArr = arr.every(function (val) {
//   return val > 3;
// }); ---- returns true/false

// Array Operators : destructuring, spread-operator

let array = [1, 2, 3, 4, 5, 6];

// let [a,b,c,d] = arr
// let newArr = [...array]
